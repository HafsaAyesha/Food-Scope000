reate and insert seed data directly into the existing MongoDB database for the FoodScope backend.

Important Constraints:

Do NOT run the application.
Do NOT start the server.
Do NOT execute any scripts or commands that perform runtime execution.
Do NOT install, update, or remove any packages.
Do NOT create separate seed files or external seed modules.
All seed data logic must be written inside a single code block that can be manually executed once.
Do NOT modify existing backend business logic or API structure.

Requirements:

Insert realistic sample data directly into MongoDB collections for:

Users
Admin user
Reviewer users
Regular users
Include hashed password format if required
Assign correct roles
Restaurants
Name, cuisine, price range
Location (lat/lng for geolocation features)
Tags
Images (placeholder URLs allowed)
Ratings
Food Items
Linked to restaurants
Name, description, price
Tags
Ratings
Reviews
Linked to users and restaurants/food items
Star ratings (1–5)
Comments
Helpful votes
Optional images
Tags
Hierarchical structure (parent-child relationships if supported)
Notifications (optional)
Sample notification entries for testing UI

MongoDB Requirements:

Use native MongoDB insert operations (or mongoose if already used in the project)
Insert directly into collections (users, restaurants, fooditems, reviews, tags, notifications)
Ensure all relationships (ObjectIds) are correctly mapped
Ensure data consistency across collections

Data Requirements:

10–20 users
10 restaurants
20–30 food items
30+ reviews
Diverse ratings, cuisines, and tags
Realistic and UI-friendly data for frontend testing

Output Requirements:

Provide a single MongoDB seed script only
Include all insert operations in one place
Clearly structured and ready to paste into a backend file or Mongo shell
Include only inline comments if necessary

Do NOT:

Split into multiple files
Run the script
Start server
Create API endpoints
Add new dependencies
Change backend architecture

Goal:
Populate the existing MongoDB database with realistic sample data so the entire FoodScope frontend can be tested visually, including search, filters, geolocation, reviews, ratings, and admin dashboard features.