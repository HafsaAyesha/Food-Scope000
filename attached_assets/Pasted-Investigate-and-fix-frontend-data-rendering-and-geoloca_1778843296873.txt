Investigate and fix frontend data rendering and geolocation issues in the FoodScope application.

Important Constraints:

Do NOT modify backend architecture unless absolutely necessary.
Do NOT change core API logic unless a clear bug is identified.
Do NOT add unnecessary libraries or rewrite the entire frontend.
Do NOT introduce complex state management or redesign the app.
Fix issues with minimal, targeted changes.
Do NOT run the application; only analyze and fix through code changes.
Problem 1: No Data Showing in Frontend

Issue:

Seed data exists in MongoDB but nothing is rendering in the frontend.
Pages like restaurants, food items, reviews, or home sections appear empty.

Tasks:

Verify API endpoints are being called correctly in frontend services.
Check axios/fetch configuration (base URL, headers, routes).
Ensure responses are correctly mapped to UI components.
Validate useEffect data fetching logic.
Confirm state is being updated properly after API calls.
Check for console errors, undefined values, or broken imports.
Ensure backend responses match frontend expectations (field names, nesting, etc.).

Expected Fix:

Data from backend should correctly render in all relevant UI sections.
Problem 2: “Show Nearby” Button Not Working

Issue:

Nearby restaurants feature does not return correct results or shows nothing.

Tasks:

Verify geolocation API usage in frontend (navigator.geolocation).
Ensure latitude and longitude are correctly captured.
Check API request payload for nearby restaurants endpoint.
Validate backend query logic expects correct coordinate format.
Ensure distance filtering logic is working (if implemented frontend or backend).
Handle permission denied or missing location cases gracefully.
Add basic debugging logs if needed (without polluting production logic).

Expected Fix:

Clicking “Show Nearby” should correctly fetch and display restaurants near the user’s actual location.
General Debug Requirements:
Identify mismatch between backend response and frontend consumption.
Ensure all data flow paths are working: API → service → state → UI.
Fix broken or missing props in components.
Ensure no silent failures in async requests.
Output Requirements:
Provide only code changes or explanations tied directly to fixes.
Clearly specify which files were modified.
Do NOT redesign UI or architecture.
Do NOT add unnecessary features.
Do NOT run the project.

Goal:
Make sure all seeded data appears correctly in the frontend and the geolocation-based "Show Nearby" feature works reliably using real user location data.